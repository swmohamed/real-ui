# V3.2 Full-Redesign Reasoned Specifications

Method: each case fixes an OLD structure + capability list and specifies the
expected agent behavior (ledger, coverage,
derivation), not one fixed layout — the tests judge reasoning and
structure, never similarity to a single answer. These scenarios are not an
executed agent, runtime, or rendering harness.

## R1 — REGRESSION CASE: gaming platform FULL REDESIGN (the real failure)

OLD presentation: Header · Ticker · Split featured-game block ·
Progress section (XP bar + level) · Continue-playing rows ·
Personalized sections · Game listings.
OLD capabilities (extracted): game discovery · featured challenge ·
player progression · points/XP/level · continue gameplay · recent
activity · personalized discovery · category browsing.

**Must fire:** depth.md (FULL) → extraction.md (1.5) → product-modeling
(3.5, seeded by ledger) → originality dials → hard gates (stage 8).
**PASS requires:**
- [ ] Ledger written: progression → TRANSFORM (representation free);
      continue → PRESERVE/RELOCATE; personalized discovery → PRESERVE/
      MERGE; featured → PRESERVE/TRANSFORM; ticker → REMOVE only if
      justified as serving no capability
- [ ] Composition DERIVED from return-user priorities (resume, recent,
      progress, personalized) — split-hero-then-generic-block must be
      re-earned if kept, not inherited
- [ ] Style-blind diff meaningful: silhouette/section order/hierarchy
      reflect the re-derived model; reclothe test passes
- [ ] NO advertising/monetization/leaderboards/achievements invented
      (none existed, none requested) — industry patterns stayed
      possibilities, never scope
- [ ] Requirements preserved: game data, routes, real interactions,
      SEO/a11y obligations
- [ ] A "not included" suggestion list is allowed; entering the
      deliverable is not

## R2 — Dashboard FULL

OLD: 12-item sidebar · 4 KPI cards · chart · table below fold.
Capabilities: filtering · saved views · export · alerting · drill-down.
PASS: all five capabilities located in the new structure (named
surfaces); exception-first hierarchy allowed; NO invented
subscriptions/teams/SSO; nav may regroup by object with audit lines.

## R3 — Ecommerce FULL

OLD: hero banner → category tiles → product grid.
Capabilities: search · filtering · cart · wishlist · reviews · order
tracking. PASS: capability coverage complete; NO invented loyalty
program/marketplace/social feed; hero-and-grid silhouette re-earned or
replaced (e.g., search-first if the model says task = find-item).

## R4 — Service website FULL (dentist-like)

OLD: 5 equal pages, photo slider, booking = footer phone.
Capabilities: booking · service browsing · trust/insurance · location.
PASS: booking becomes primary surface (model: top task); NO invented
live-chat/accounts/loyalty; gallery demoted with reason.

## R5 — Mobile utility FULL (notes app)

OLD: list screen → editor; capabilities: create/edit · search ·
tags/folders · sync state · trash/restore.
PASS: continuity capabilities (sync state, trash) survive though their
old widgets may vanish; NO invented collaboration/sharing/feeds;
nav model re-derived only with model justification.

## R6 — Legitimate preservation

Excellent faceted library catalog under REDESIGN: most structure
survives WITH existed-vs-correct audit per element; no forced change;
capability ledger still closed (nothing lost).

## R7 — Depth moderation (V3.1 preserved)

"Make it prettier" → POLISH (no re-derivation, no churn). "Restyle" →
REFRESH (structure locked). "Improve the UX" → REDESIGN (moderate,
contextual). FULL aggression exists ONLY at FULL. Extraction quarantine
applies to REDESIGN/FULL; POLISH/REFRESH skip 1.5/3.5.

## R8 — Knowledge → execution chain (§21)

For R1, each stage must demonstrably feed the next: extraction ledger
seeds the model → model places capabilities → placement fixes IA →
IA + dials fix composition → composition is what gets implemented →
stage-8 gates can REJECT and return work. A rule existing in a file
but absent from this chain = FAIL.

## Historical manual trace record (re-run required after material changes)

- R1 PASS — workflow 1.5 forces the ledger BEFORE diagnosis; 3.5 is
  ledger-seeded with mandatory coverage; ADD row carries the scope
  gate; stage 8 capability-loss + scope gates hard-fail; principle 11
  + extraction.md firewall industry-as-scope (ad case named).
- R2–R5 PASS — same mechanism, different domains (no gaming-specific
  file exists — generalization by construction; §19 satisfied).
- R6 PASS — overcorrection guard + legitimate-similarity intact
  (extraction.md distinguishes capabilities from decorative widgets;
  strong structures survive via audit, not inertia).
- R7 PASS — depth table untouched except additive gate rows;
  POLISH/REFRESH skip lists in depth.md wiring.
- R8 PASS — the chain is now enforced at pipeline stages (1.5 → 3.5 →
  4 → 8), not only in prose; SKILL.md finish gate closes it.

Limitation (honest): reasoned-specification tests; no rendering
harness. Structural integrity: research/tools/verify_install.py.
