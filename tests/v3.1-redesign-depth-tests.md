# V3.1 Redesign-Depth Reasoned Specifications

Method: each scenario fixes an OLD structure and specifies what an agent run
should produce (or defend keeping). It is not an executed agent/render test.
PASS in the historical record means a prior manual trace found the result
structural change forced by routed rules, not by luck. Reasoned tests
(honest: no rendering harness) — every assertion names its forcing file.

## T1 — Visually outdated, functionally sound website (dental clinic)

OLD: 5 equal pages (Home/About/Services/Gallery/Contact) · hero photo
slider · services as prose list · booking = phone number in footer.

**FULL REDESIGN** — must fire: depth.md (FULL) → product-modeling (3.5)
→ preservation (requirements only).
- [ ] Model derived first: patients/appointments/treatments/insurance;
      top task = book appointment
- [ ] New IA is task-priority, not company-chronological: booking
      module above fold on home; treatments as scannable grid (duration/
      price); insurance/trust strip; About demoted
- [ ] Style-blind diff: section order, silhouette, primary action ALL
      changed — reclothe test passes (old skin on new structure ≠ old site)
- [ ] Requirements preserved: treatment content, phone booking path,
      routes mapped (301s), NAP/SEO blocks

**REDESIGN (same site, lesser depth)** — structure largely kept:
- [ ] Hierarchy fixes only: booking CTA promoted to header, services
      prose → grid; nav order unchanged — depth-appropriate moderate diff

## T2 — Generic card-heavy interface (internal ops dashboard)

OLD: 9 equal cards mixed (stats, task lists, alerts, links) in a 3×3 grid.

**REDESIGN** — must fire: cards.md when-NOT + product-modeling + depth.
- [ ] Mixed entities ≠ homogeneous → cards dissolve: KPI stat strip
      (monitoring verb) + tasks table (dense/comparable) + alerts feed
      (chronological) — container TYPES change, not just styling
- [ ] Grouping follows tasks; every dissolution cites the decision row

**FULL** — additionally: app shell re-derived; nav grouped by workflow
stage, not by team org chart.

## T3 — SaaS analytics dashboard

OLD: 12-item flat sidebar · 4 equal KPI cards · one chart · recent
table below fold.

**REDESIGN** — must fire: pages/dashboard + product-modeling + depth.
- [ ] Top task = spot anomalies → exception-first hierarchy (alert
      summary leads; KPIs contextualized with deltas)
- [ ] Sidebar regrouped BY OBJECT with reasons (frequency × task);
      entity surfaces all survive (no data lost)

## T4 — Content/media product (video portal)

OLD: uniform 4-col equal thumbs · channels buried in a dropdown.

**REDESIGN** — must fire: originality.md rhythm dial + industries/
entertainment-streaming.
- [ ] Content rhythm = editorial mixed → silhouette changes to
      lead-feature + rails (series / continue-watching surfaced —
      resume is a top task); uniform grid does NOT survive by default

## T5 — Mobile product (food delivery app)

OLD: 5 tabs (Home/Search/Cart/Orders/Profile) · home = promo carousel first.

**REDESIGN** — must fire: devices/mobile + ux/mobile-states + depth.
- [ ] Top task = fast reorder → home re-prioritized: address/delivery-
      time header, cuisine rail, reorder strip ABOVE promos
- [ ] Tab verdicts written per element with existed-vs-correct reasons
**FULL** — nav model re-derived (e.g., 5→4 tabs, search folded into
persistent home search) ONLY if the model justifies; never random.

## T6 — Overcorrection guard (excellent IA)

Scenario: library catalog with superb faceted IA, dated visuals, under
REDESIGN. PASS: most structure SURVIVES with existed-vs-correct audit
lines per element; no forced difference (depth.md guard +
originality.md legitimate-similarity). FAIL if anything moves without
a reason.

## T7 — Depth classification guard

"Make it prettier / clean up" must classify POLISH or REFRESH — NOT
trigger structural re-derivation (no churn). "Rethink this product
from scratch" must classify FULL — NOT polish-with-courage.
Signals table (depth.md) is the forcing artifact.

## T8 — V3 regression

- [ ] product-modeling still routes in fresh designs (map row intact)
- [ ] originality dials feed the NEW composition (depth.md references)
- [ ] legitimate similarity allowed (T6)
- [ ] non-redesign workflow untouched (SKILL.md design-path rows
      unchanged — only redesign rows gained depth.md)
- [ ] normal/deep-audit routing, RTL gates, finish-gate items all intact

## Historical manual trace record (re-run required after material changes)

- T1 PASS — depth.md FULL row + workflow 3.5 force model-first IA;
  reclothe + style-blind diff enforce structural change; preservation
  table's requirement rows protect content/routes.
- T2 PASS — cards.md decision table ("mixed entities ≠ homogeneous")
  forces container change; depth REDESIGN row unlocks it.
- T3 PASS — dashboard exception-first follows product-modeling task
  ranking; nav regroup required by existed-vs-correct audit.
- T4 PASS — rhythm dial (uniform→editorial) is a structural dial now;
  industry file's resume-task evidence justifies continue-watching.
- T5 PASS — task-priority home forced by modeling; tab verdicts audited.
- T6 PASS — overcorrection guard + legitimate-similarity rule present.
- T7 PASS — signals table + ambiguity rule (state assumed depth).
- T8 PASS — additive edits only (grep-verified: SKILL.md design rows
  unchanged; product-modeling/originality untouched this pass).

Limitation (honest): reasoned-specification testing; structural
integrity via verify_install.py.
