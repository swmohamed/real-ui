# V7.1 Cumulative Real-Product Evidence + Modern Quality Upgrade

Date: 2026-09-03. Commit base: `049795e` (V7 + variance stress test).
Method: AUDIT → EXPAND CORPUS → COMPARE → UNDERSTAND WHY → EXTRACT →
STORE → ROUTE → VALIDATE, per the upgrade doctrine. All corpus growth is
development-time; normal user flow is unchanged (retrieve stored knowledge,
reason, design — SKILL.md Normal mode contract untouched).

## Audit result (before this upgrade)

Per-category evidenced references (v2 corpus + module-observed + V7 doc
studies): 7 categories ≥ 20; 17 below floor; worst: islamic-apps 0,
jobs-recruitment 1, logistics-delivery 2, crypto-web3 2.

## Corpus expansion (the campaign)

- 8 fetch waves (`research/raw/w*.json` manifests → `research/reports/w*.json`),
  ~265 candidate real products across all weak categories, chosen for
  PRODUCT-MODEL DIVERSITY (audience/business/model/region/platform axes),
  not volume.
- 264 successful SOURCE-OBSERVED fetches retained (HTML fetched + structure
  evidence per site: nav sample, heading map, form/input counts, dir/lang,
  framework hints). Failures (bot walls/timeouts) logged per wave file.
- Final ledger (`research/reports/evidence-ledger.md`, regenerable via
  `research/tools/evidence_ledger.py`): **533 unique evidenced references,
  ALL 24 categories ≥ 20** (floor = minimum, per permanent rule).

## Synthesis stored (WHY/WHEN/WHEN-NOT/TRADEOFF — never "category uses X")

17 industry modules extended with "Corpus observations (v7.1 growth)"
sections: family tables (observed shape / why it differs / when & when
NOT) for islamic-apps, jobs-recruitment, logistics-delivery, crypto-web3,
science-utility, government-public, automotive, real-estate, education,
entertainment-streaming, sports-fitness, travel-tourism, restaurants-food,
social-community, creative-culture, fashion-luxury-beauty, b2b-enterprise,
immersive-experimental. Key generalizations: payer splits the surface
(logistics/jobs); social OBJECT decides structure (feeds vs artifact
communities); query shape defines travel surfaces; utility plainness is a
legitimate professional register (generic-polish list); government = 4
families, not one pattern; crypto = 5 families sharing almost nothing.

## Modern/professional quality intelligence

- NEW `knowledge/foundations/modern-craft.md`: evidence-backed craft
  decision logic — typography discipline (roles/scale/weight/leading/
  numerals), spacing rhythm, geometry register (2 radii max), color
  restraint, surface/elevation hierarchy, imagery-role choice, motion
  budget, production-ready checklist. Explicitly NOT a universal style;
  modern = discipline, not decoration.
- `anti-patterns/ai-aesthetics.md` extended: generic-polish list (the
  "looks clean but is template gravity" patterns: uniform radius, 600-
  weight sameness, shadow-on-every-card, SaaS default stack, etc.).

## Routing/activation

- SKILL.md map: modern-craft row + craft finish-gate check; evidence row
  points at the ledger (development-time only).
- Permanent cumulative-growth rule encoded in `research/method.md`
  (v7.1 section): future upgrades must grow diversity, never reset, never
  stop at the floor, honesty ledger discipline (CATALOGED ≠ counted).

## Validation (stored knowledge only, no re-research) — PASS

Three fresh products designed purely from the NEW stored knowledge,
RENDERED at 1280×800 + 390×844, 0 console errors, 0 overflow (after one
repair), 0 unlabeled controls, 0 nameless controls, landmarks + 1×h1 each:

1. `crypto-terminal.html` — crypto-web3 data-terminal family: dense
   sortable TVL table, tabular numerals, monitoring-dark (justified by
   corpus family), methodology honesty, near-flat surfaces, 4/6px radius
   register. NOT an exchange trust page, NOT a wallet landing — family
   knowledge chose the structure.
2. `prayer-rtl.html` — islamic-apps prayer-utility family: one glance
   answer (next prayer + countdown), location-first, method transparency
   (calculation authority named, per module contract), explicit scope
   honesty ("prayer times only — Quran/Qibla are other products"),
   Arabic-first typography (Tajawal stack, 1.85 leading, LTR-isolated
   numerals, Hijri date).
3. `parcel-tracker.html` — logistics tracking-hub family: tracking widget
   IS the hero (zero browsing intent), carrier auto-detection, honest
   carrier-status language (never paraphrased), worked example with
   customs-hold truth state.

Failures found & repaired: crypto table overflowed at 390px → contained
`.table-scroll` + min-width (per responsive rule: tables keep comparison
via priority + scroll) → retest PASS. One tool-written file vanished
post-write (recurring environment issue, also seen once in the variance
session) → rewritten + verified on disk; noted as tooling, not knowledge.

Anti-template check: three outputs share zero structure (terminal table /
glance card / tracking form), each family-correct — knowledge expanded the
solution space instead of templating it.

## Regression

- 29/29 invariant tests OK; verify_install: 19/19 destinations IDENTICAL
  after re-sync; no ghosts/orphans; YAML safe; README counts updated
  (127 knowledge files); patch hygiene clean.

## Remaining gaps (honest)

Luxury-house + mega-marketplace walls (covered via module-observed names
only); native app-store surfaces (DOC-OBSERVED platform docs remain the
path); some SPA sites return thin static shells (partial structure
evidence); CSS-token deep pass on wave sites pending (fetch_analyze full
mode); Asia/South-America regional coverage thin; per-product a11y quality
signals not yet extracted. All recorded in the ledger's gap section as
next-upgrade candidates per the permanent rule.
